# Codebase Review - 2026-02-21

**Mode**: full
**Scope**: Entire codebase (14 PHP files, 2 JS files, 1 CSS file, 1 build script)

## Executive Summary

| Category          | P0 | P1 | P2 | P3 | Total |
| ----------------- | -- | -- | -- | -- | ----- |
| Logic Correctness | 1  | 1  | 3  | 1  | 6     |
| Performance       | 0  | 0  | 5  | 2  | 7     |
| Error Handling    | 0  | 2  | 4  | 3  | 9     |
| Security          | 1  | 1  | 4  | 4  | 10    |
| Architecture      | 0  | 0  | 5  | 3  | 8     |
| **Total**         | **2** | **4** | **21** | **13** | **40** |

## Regression Status

No previous review exists. This is the baseline review.

---

## Findings

### Category: Logic Correctness

| ID | Priority | Location | Issue | Recommendation | Verify Exists | Verify Fixed |
| -- | -------- | -------- | ----- | -------------- | ------------- | ------------ |
| L1 | P0 | `README.md` (entire file) | README describes v0.x mirror-domain architecture (append `.json`, no caching, mirror domain config) but code implements v1.0.0 push-sync + locus-core API model. README is completely wrong for current version. Installation/config guidance will misconfigure the plugin. | Rewrite README for v1.0.0: update Description, Features, Configuration, How It Works, Developer Hooks, Changelog. Remove mirror-domain references. | `grep "mirror" README.md` returns hits; `grep "locus_api_url" README.md` returns nothing | README describes `locus_api_url`, `connector_url`, `api_key`, push sync, caching |
| L2 | P1 | `class-botdot-wp.php:177` vs `class-botdot-wp-public.php:76` | Two separate `BotDot_WP_Content_Injector` instances exist: one for hooks (wp_head/the_content/wp_footer), one for shortcode. `$appendix_injected` and `$shortcode_used` flags are per-instance, so shortcode rendering doesn't prevent duplicate injection from the hook instance. Results in double appendix HTML on pages using shortcode/block with `injection_position=bottom`. | Share a single Content_Injector instance between the hook registrations and BotDot_WP_Public. | Insert `[botdot_appendix]` shortcode on a page with injection_position=bottom; view source shows appendix content twice | Appendix appears only once regardless of placement method |
| L3 | P2 | `README.md:83,98` | Filter `botdot_wp_inject_on_archives` documented in README but does not exist anywhere in the codebase. Archives are silently excluded in `is_valid_page_type()` with no override. | Remove from README or implement the filter in `is_valid_page_type()` | `grep -r "inject_on_archives" .` returns 0 PHP hits | Filter applied in `is_valid_page_type()` or removed from docs |
| L4 | P2 | `class-botdot-wp-sync.php:458-475` | `_botdot_sync_word_count` never initialized on first sync. On second save, `compute_change_percentage()` finds no stored word count and returns 100%, always bypassing threshold. Threshold only works from third save onward. | Write `_botdot_sync_word_count` after first successful sync in `on_save_post` | Save a post twice with minor edits; second save always triggers sync regardless of threshold setting | Second save respects threshold; word count initialized on first sync |
| L5 | P2 | `README.md:18` vs `class-botdot-wp-content-fetcher.php:51-66,119-121` | README says "No caching (always fetches fresh data)" but Content_Fetcher uses transient caching with configurable TTL (default 3600s). | Update README to describe caching behavior | `grep "No caching" README.md` returns hit; `grep "set_transient" class-botdot-wp-content-fetcher.php` returns hit | README accurately describes caching with TTL |
| L6 | P3 | `class-botdot-wp-loader.php:63` vs `class-botdot-wp.php:215-217` | Loader docblock types `$component` as `object` but sync hooks pass string `'BotDot_WP_Sync'`. Works for static methods but violates documented contract. | Register sync hooks directly via `add_action()` or instantiate BotDot_WP_Sync | `grep "'BotDot_WP_Sync'" class-botdot-wp.php` shows string usage | Sync hooks registered consistently with loader pattern |

### Category: Performance

| ID | Priority | Location | Issue | Recommendation | Verify Exists | Verify Fixed |
| -- | -------- | -------- | ----- | -------------- | ------------- | ------------ |
| P1 | P2 | `class-botdot-wp-content-fetcher.php:53-66` + `class-botdot-wp-content-injector.php:93,155` | `fetch()` called 2x per page load (wp_head + the_content). Each cache hit unconditionally calls `check_freshness()` which makes an HTTP request to `/appendix/check`. Minimum 2 outbound HTTP requests per cached page view. | Add static per-request in-memory cache in `Content_Fetcher::fetch()` keyed by `$url_path`. | Profile a page load; observe 2 HTTP calls to locus-core per request | Only 1 HTTP call per page load on cache hit |
| P2 | P2 | `class-botdot-wp-sync.php:308-342,384-391` | Bulk sync: N+1 queries. First loop: `get_post()` + 4 meta queries per post. Second loop: redundant `get_post()` + 3 `update_post_meta()` per post. Total: `1 + 9N` queries. 1000 posts = 9001 queries. | Cache post objects from first loop; compute hash before HTTP call; eliminate second `get_post()` loop | Run bulk sync with query logging; count queries | Queries reduced to `1 + 5N + 3N` updates |
| P3 | P2 | `class-botdot-wp-sync.php:308-372` | Bulk sync loads ALL published posts (`posts_per_page=-1`) with full `post_content` into single JSON payload. 1000 posts at 5KB each = 5MB content + PHP array overhead. No chunking, 60s timeout. | Chunk in batches of 100; consider background WP-Cron job | Run bulk sync on site with 500+ posts; observe memory/timeout | Batched requests with configurable chunk size |
| P4 | P2 | `class-botdot-wp-content-injector.php:301-304` | `page_injection_status` stored as single autoloaded `wp_option`. Grows unbounded (one entry per toggled page). Deserialized on every frontend request. Full array rewritten on every toggle AJAX. | Store as post_meta `_botdot_inject_enabled` per post, or mark option `autoload=no` | `SELECT LENGTH(option_value) FROM wp_options WHERE option_name='botdot_wp_page_injection_status'` | Per-post meta or non-autoloaded option |
| P5 | P2 | `botdot-wp-admin-settings.php:53-78` | Sync tab fires 3 identical `COUNT(DISTINCT)` queries + N `wp_count_posts()` calls. Consolidatable to 2 queries. | Single `GROUP BY meta_value` query + single `GROUP BY post_type` count | Enable query logging; load sync tab; count queries | 2 queries instead of 3+N |
| P6 | P3 | `class-botdot-wp-content-fetcher.php:256-259` | `clear_cache(null)` uses raw SQL `LIKE` scan on `wp_options`. On large tables (10k+ rows), can take 100-500ms with table lock. | Maintain registry of transient keys or use object cache groups | Run on site with large wp_options table; measure query time | Indexed key-based deletion |
| P7 | P3 | `botdot-wp-admin-settings.php:166-169` | All `page_injection_status` entries emitted as hidden `<input>` fields on Connection/Sync tabs. 500 overrides = 500 inputs, ~90KB extra HTML. | Use single JSON hidden field (pattern already exists on display tab) | View source of Connection tab with many page overrides; count hidden inputs | Single JSON field or separate settings groups |

### Category: Error Handling

| ID | Priority | Location | Issue | Recommendation | Verify Exists | Verify Fixed |
| -- | -------- | -------- | ----- | -------------- | ------------- | ------------ |
| E1 | P1 | `class-botdot-wp-content-fetcher.php:161-174` | `check_freshness` returns `false` (stale) on ANY error — endpoint down, non-200, invalid JSON. Forces full re-fetch on every page load if check endpoint is flaky. Self-amplifying load spike: degraded check endpoint eliminates cache entirely. | Return `true` (treat as fresh) on check failure. Add short-lived transient to skip checks after recent failure. | Take down `/appendix/check` endpoint; observe every page load doing full `/appendix/render` fetch | Cache remains valid when check endpoint fails |
| E2 | P1 | `class-botdot-wp-admin.php:584-589` | `handle_sync_status`: `is_wp_error` branch calls `wp_send_json_error()` without `return`. Execution falls through to `wp_send_json_success($body)` with `$body=null`. Double-send on every WP_Error. | Add `return;` after `wp_send_json_error()` | Trigger a connection error in sync status check; observe double JSON response | Single JSON response on error |
| E3 | P2 | `class-botdot-wp-sync.php:115-123` + `class-botdot-wp-admin.php:164-200` | Silent sync failure on post save. `send_webhook()` returns false, meta set to 'error', but no feedback to user in post editor. Admin notices only show on settings page and dashboard, not post.php. | Show sync failure notice on post edit screen | Save a post when connector is down; observe no error feedback in editor | Error notice shown in post editor after failed sync |
| E4 | P2 | `class-botdot-wp-sync.php:257-286` | No retry for webhook failures. Single HTTP attempt; transient network errors permanently mark post as 'error' with no automated recovery. | Schedule WP-Cron retry with exponential backoff (5min, 30min, 2h). Cap at 3 retries. | Simulate network failure on webhook; observe no retry | Failed syncs retried via cron |
| E5 | P2 | `class-botdot-wp-admin.php:461-463,490-498` | Race condition in `page_injection_status` read-modify-write. Two simultaneous AJAX toggles: both read same snapshot, last write wins, first toggle silently dropped. | Use post_meta per page (eliminates shared state) or add transient mutex | Rapidly toggle two different pages simultaneously | Each toggle persists independently |
| E6 | P2 | `class-botdot-wp-admin.php:370,434,450,479,514,541,565` | All AJAX handlers call `wp_send_json_error()` after permission check without explicit `return`. Relies on `wp_send_json_error` internally calling `wp_die()`. | Add `return;` after every `wp_send_json_error()` call | Code inspection: no `return` after `wp_send_json_error` | Every `wp_send_json_error()` followed by `return;` |
| E7 | P3 | `class-botdot-wp-content-fetcher.php:82-103` | Stale cache served silently on fetch failure. No signal to admin or visitor that content is stale. | Return `stale` flag; in debug mode inject HTML comment; admin bar indicator for logged-in users | Disconnect locus-core; observe cached content served with no indication | Stale indicator in debug mode |
| E8 | P3 | `class-botdot-wp-logger.php:133` + `class-botdot-wp-admin.php:179,240` + `botdot-wp-admin-settings.php:95` | `current_time('timestamp')` deprecated since WP 5.3. Produces timezone-offset timestamps. | Replace with `time()` for UTC timestamps | `grep "current_time.*timestamp" includes/ admin/` shows hits | Uses `time()` or `current_datetime()->getTimestamp()` |
| E9 | P3 | `class-botdot-wp-admin.php:263` | Nonce in meta box inline JS generated at render time. If admin page is object-cached, stale nonce served, "Sync Now" button fails silently. | Use `wp_localize_script()` instead of inline PHP-in-JS | Cache admin page; try "Sync Now" after cache — nonce fails | Nonce generated via localized script |

### Category: Security

| ID | Priority | Location | Issue | Recommendation | Verify Exists | Verify Fixed |
| -- | -------- | -------- | ----- | -------------- | ------------- | ------------ |
| S1 | P0 | `class-botdot-wp-content-injector.php:161-168,216-224,249-256` | **Unsanitized external HTML injection.** `$data['html']` from locus-core API echoed/appended directly to page content with zero sanitization. If locus-core is compromised, arbitrary HTML/JS injected on every page for every visitor. `inject_appendix_content()` appends to `$content`; `inject_above_footer()` echoes directly; `render_shortcode()` returns directly. | Pass `$html` through `wp_kses_post()` or strict allowlist before injection | `grep -n 'echo \$html' class-botdot-wp-content-injector.php` shows raw echo at line 223 | All injected HTML passes through `wp_kses_post()` |
| S2 | P1 | `class-botdot-wp-content-injector.php:109-113` | **JSON-LD script-tag breakout.** When `$jsonld` is a string, it's echoed directly in `<script>` tag. If API returns `</script>` in a JSON value, it breaks out of the script tag allowing arbitrary HTML/JS injection. `wp_json_encode` with `JSON_UNESCAPED_SLASHES` does not escape `<`, `>`, `/`. | Always re-encode through `wp_json_encode()` or `str_replace('</script>', '<\/script>', $json_string)` | Craft API response with `</script><script>alert(1)</script>` in a JSON-LD value; observe script breakout | No `</script>` sequences possible in output |
| S3 | P2 | `botdot-wp-admin-settings.php:194,201,208` | API keys and webhook secret rendered as plaintext in `<input type="password" value="...">`. Visible in HTML source, accessible to browser extensions, XSS chains. | Show masked placeholder; only reveal on demand via authenticated AJAX | View page source of settings page; search for key values | Keys not in HTML source; revealed only on explicit request |
| S4 | P2 | `class-botdot-wp-sync.php:280-285` | API response body logged verbatim via `wp_remote_retrieve_body($response)`. If remote server reflects auth headers in error responses, secrets could enter error logs. | Truncate/sanitize response body before logging | Check error log after webhook failure for sensitive data | Response body truncated to 500 chars in logs |
| S5 | P2 | `class-botdot-wp-admin.php:650-658` | `sanitize_page_injection_status` reads raw `$_POST['botdot_wp_page_injection_status_json']` directly, bypassing WordPress settings API flow. Merged without nonce verification at that call point. | Remove direct `$_POST` access; use single canonical field | Code inspection: `$_POST` accessed in sanitize callback | No direct `$_POST` access in sanitize callbacks |
| S6 | P2 | `class-botdot-wp-sync.php:248-251` (outbound only) | No inbound HMAC signature verification. Plugin sends `X-WP-Signature` but never verifies incoming requests. If inbound webhook handler is added without verification, it will be unauthenticated. | Add `verify_incoming_signature()` helper; document requirement for future inbound routes | `grep -r "verify.*signature\|check.*signature" .` returns nothing | Verification helper exists and documented |
| S7 | P3 | `class-botdot-wp-content-fetcher.php:74-80` + `class-botdot-wp-sync.php:257-262` | **SSRF via admin-configured URLs.** Admin can set API URLs to internal services (AWS metadata, etc.). Plugin makes server-side HTTP requests to those URLs with API key attached. | Validate URLs against RFC-1918, loopback, link-local, metadata service ranges | Set `locus_api_url` to `http://169.254.169.254/latest/meta-data/`; observe request made | URL validation blocks internal/metadata addresses |
| S8 | P3 | `botdot-wp-admin-settings.php:542,583,607,637,680` + `class-botdot-wp-admin.php:263` | Nonces embedded in inline JS. If admin pages are CDN/fragment cached, stale nonces exposed. Replay window ~12h. | Use `wp_localize_script()` for nonce delivery; set `Cache-Control: no-store` on admin pages | Check for nonce values in inline `<script>` blocks | Nonces delivered via localized scripts |
| S9 | P3 | `class-botdot-wp-content-fetcher.php:256-259` + `uninstall.php:54-56` | SQL queries use raw strings without `$wpdb->prepare()`. Pattern is hardcoded (safe today) but sets dangerous precedent. | Wrap in `$wpdb->prepare()` or add comment explaining safety | Code inspection: `$wpdb->query()` without `prepare()` | Queries use `$wpdb->prepare()` |
| S10 | P3 | `botdot-wp-admin-settings.php:421,486` | `paginate_links()` output echoed without `wp_kses_post()`. Low risk under current code but no defense-in-depth. | Wrap in `wp_kses_post()` | `grep "echo \$page_links" botdot-wp-admin-settings.php` | Output wrapped in `wp_kses_post()` |

### Category: Architecture

| ID | Priority | Location | Issue | Recommendation | Verify Exists | Verify Fixed |
| -- | -------- | -------- | ----- | -------------- | ------------- | ------------ |
| A1 | P2 | `THEME-INTEGRATION.md` (entire file) | Documents features that don't exist: theme auto-detection, `botdot_wp_theme_classes` filter, `botdot_wp_appendix_args` filter, `use_theme_classes` flag, 3-argument `botdot_wp_appendix_html` filter, debug comment. None exist in codebase. Shipped in production builds. | Update to document actual features or remove. Do not ship docs for non-existent APIs. | `grep -r "theme_classes\|appendix_args\|use_theme_classes" includes/ public/` returns 0 hits | Doc matches actual implementation |
| A2 | P2 | `public/js/botdot-wp-tinymce.js:19-49` + `public/js/botdot-wp-gutenberg.js:22-31` + `class-botdot-wp-content-injector.php:235` | TinyMCE and Gutenberg both collect `title` and `open` attributes. `render_shortcode($atts)` ignores `$atts` entirely. Gutenberg PHP registration uses empty `attributes` array. User-configured values have no effect. | Either honor attributes in render or remove from UI | Insert `[botdot_appendix title="Custom" open="true"]`; output is identical to `[botdot_appendix]` | Attributes affect rendering or are removed from UI |
| A3 | P2 | `README.md:103-110` | Changelog only has entry for v0.1.0. Eight versions (0.2.0-1.0.0) have no changelog entries despite `@since` tags referencing them. | Add changelog entries for all released versions | `grep "@since" includes/ public/ admin/` shows 0.2.0, 0.3.0, 1.0.0 references | Changelog covers all released versions |
| A4 | P2 | `class-botdot-wp.php:74` | `define_admin_hooks()` instantiates `BotDot_WP_Admin` and registers 14 hooks on every page load including frontend. Admin AJAX handlers registered on public requests unnecessarily. | Wrap in `if (is_admin())` | Code inspection: no `is_admin()` guard in constructor | Admin hooks only registered in admin context |
| A5 | P2 | `class-botdot-wp-admin.php:650-658` | `sanitize_page_injection_status` reads raw `$_POST` in sanitize callback. Companion `_json` sanitizer always returns empty string (no-op carrier). Bypasses WordPress settings API data flow. | Consolidate to single field; remove `$_POST` access from sanitize callbacks | Code inspection: `$_POST` in sanitize callback | Single canonical field without `$_POST` access |
| A6 | P3 | `build.sh:49` + `testing/debug-check.sh` | `debug-check.sh` bundled in production builds. Checks for stale v0.x file names (`class-botdot-wp-fetcher.php`, `class-botdot-wp-injector.php`) that no longer exist. Script always reports errors. | Update file list in script; exclude from production builds | Run `./debug-check.sh`; observe false "missing" errors | Script excluded from dist or checks current file names |
| A7 | P3 | `class-botdot-wp-sync.php:82-113` | `_botdot_sync_word_count` never initialized on first sync. Only written inside `compute_change_percentage()` which is only called when `$previous_hash` exists. Second save always returns 100% change. | Write word count after first successful sync | Save new post, then make minor edit; observe sync fires despite threshold | Word count set on first sync |
| A8 | P3 | Across codebase | Inconsistent class patterns: Sync/Fetcher/Options/Logger use static methods. Injector/Admin/Public use instance methods. Loader accepts both strings and objects as `$component` without documentation. | Document the pattern choice; standardize where practical | Code inspection | Consistent pattern or documented rationale |

---

## Priority Definitions

| Priority | Criteria | Examples |
|----------|----------|---------|
| **P0** | Data corruption, security vulnerability, crash | Unsanitized HTML injection from external API, XSS |
| **P1** | Logic incorrectness, >10x perf degradation, frequent failures | Duplicate injection, cache-miss cascade, control-flow bug |
| **P2** | Missing validation, architectural drift, edge case failures | Missing word count init, stale docs, race conditions |
| **P3** | Code quality, minor optimization, documentation gaps | Deprecated API usage, dead attributes, inconsistent patterns |
